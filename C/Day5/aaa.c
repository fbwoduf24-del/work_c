/*
#include <stdio.h>
int sum(int x, int y); //sum이라는 함수 선언
                       //sum 함수 정의는 main 밑에 있어서 main 위에 함수 선언 

int main()
{
	int a=10,b=20;
	int result;
	
	result = sum(a,b); //sum 함수를 호출하면서 a,b값을 함수에 입력값을 준다. 
	                   //함수로부터 리턴되는 결과값을 result변수에 저장 
	printf("result = %d", result);
	
	return 0;
}

int sum(int x, int y) //함수 정의(입력, 출력), x,y에 입력을 받고, int형 출력 형태 
{
	int temp;
	
	temp = x+y; //temp 변수에 입력받은 x,y + 
	 
	return temp; //temp 출력 
}
*/
