#include <stdio.h>
/*
int main()
{
	int sum1 = 0, sum2=0;
	int ii=0, i=0, j=0;
	char ch[100];
	
	printf("반복할 문자를 쓰세요 ");
	scanf(" %s", ch);
	
	printf("몇회 반복할건지 숫자를 입력하세요 ");
	scanf("%d", &ii);
	
	for (i=1; i<=ii; i++)
	{
		printf("%s ", ch);
	}
	printf("\n\n");
	
	for (i=1; i<=100; i++) //변수 i는 1부터 시작해서 101이 됐을때는 조건이 
	{                      //조건이 충족되지 않아 연산 안됨 
		sum1=sum1+i; //i초기값 1부터 100까지 더함 
	}

	while (j<100) //
	{
		j=j+1; //j에 1씩 누적해서 더해줌 
		sum2=sum2+j; //sum2에 j를 누적해서 더해줌 
	}
	
	printf("%d         %d\n", sum1,i);
	printf("%d         %d\n", sum2,j);
}
*/



